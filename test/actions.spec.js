import {
  startAddContactDialog,
  requestAddContact,
  requestData,
  requestLogin,
  requestSignup,
  requestLogout,
  requestRemoveContact,
  requestAddBoat,
  requestBoats,
  requestRemoveBoat,
  requestAddDestination,
  requestDestinations,
  requestRemoveDestination,
  startAddTripDialog,
  requestAddTrip,
  requestRemoveTrip,
  requestJoinTrip
} from "../src/app/logic/actions";

describe("::startAddContactDialog", () => {
  test("should be a function", () => {
    expect(typeof startAddContactDialog).toBe("function");
  });
});

describe("::requestAddContact", () => {
  test("should be a function", () => {
    expect(typeof requestAddTrip).toBe("function");
  });
});

describe("::requestData", () => {
  test("should be a function", () => {
    expect(typeof requestData).toBe("function");
  });
});

describe("::requestLogin", () => {
  test("should be a function", () => {
    expect(typeof requestLogin).toBe("function");
  });
});

describe("::requestSignup", () => {
  test("should be a function", () => {
    expect(typeof requestSignup).toBe("function");
  });
});

describe("::requestLogout", () => {
  test("should be a function", () => {
    expect(typeof requestLogout).toBe("function");
  });
});

describe("::requestRemoveContact", () => {
  test("should be a function", () => {
    expect(typeof requestRemoveContact).toBe("function");
  });
});

describe("::requestAddBoat", () => {
  test("should be a function", () => {
    expect(typeof requestAddBoat).toBe("function");
  });
});

describe("::requestBoats", () => {
  test("should be a function", () => {
    expect(typeof requestBoats).toBe("function");
  });
});

describe("::requestRemoveBoat", () => {
  test("should be a function", () => {
    expect(typeof requestRemoveBoat).toBe("function");
  });
});

describe("::requestAddDestination", () => {
  test("should be a function", () => {
    expect(typeof requestAddDestination).toBe("function");
  });
});

describe("::requestDestinations", () => {
  test("should be a function", () => {
    expect(typeof requestDestinations).toBe("function");
  });
});

describe("::requestRemoveDestination", () => {
  test("should be a function", () => {
    expect(typeof requestRemoveDestination).toBe("function");
  });
});

describe("::startAddTripDialog", () => {
  test("should be a function", () => {
    expect(typeof startAddTripDialog).toBe("function");
  });
});

describe("::requestAddTrip", () => {
  test("should be a function", () => {
    expect(typeof requestAddTrip).toBe("function");
  });
});

describe("::requestRemoveTrip", () => {
  test("should be a function", () => {
    expect(typeof requestRemoveTrip).toBe("function");
  });
});

describe("::requestJoinTrip", () => {
  test("should be a function", () => {
    expect(typeof requestJoinTrip).toBe("function");
  });
});

// describe("::", () => {
//   test("should be a function", () => {
//     expect(typeof ).toBe("function");
//   });
// });
